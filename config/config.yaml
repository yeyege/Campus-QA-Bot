# 校园答疑智能客服配置

# LLM 配置
llm:
  provider: xiaomi
  model: mimo-v2.5-pro
  temperature: 0.7
  max_tokens: 4096

# 向量数据库配置
vector_db:
  collection_name: campus_faq
  persist_directory: knowledge_base/processed

# 检索配置
retriever:
  top_k: 3

# API 配置
api:
  host: 127.0.0.1
  port: 7860
